{
  "name": "form-guard",
  "version": "0.0.12",
  "description": "An clean api for validating forms",
  "main": "dist/index.js",
  "scripts": {
    "compile": " babel --presets=es2015 -d dist src",
    "prepair": "npm run compile",
    "test": "mocha --compilers js:babel-core/register **/*spec.js"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "files": [
    "dist/*.js",
    "dist/*/*.js"
  ],
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "devDependencies": {
    "expect": "^23.3.0",
    "mocha": "^5.2.0"
  }
}
